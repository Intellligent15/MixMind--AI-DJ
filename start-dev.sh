#!/usr/bin/env bash
# Placeholder. Filled in during Phase 1 (infra skeleton).
# Will: docker compose up -d postgres redis backend frontend,
#       then start the native Celery worker via `cd backend && uv run celery ...`.
set -euo pipefail
echo "start-dev.sh not yet implemented — see Build Phase Order, phase 1."
exit 1
